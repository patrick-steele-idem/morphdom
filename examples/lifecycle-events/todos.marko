div#todos
    ul if(notEmpty(data.todos))
        for(todo in data.todos)
            li class=["todo", todo.visible && 'todo-visible'] id='todo${todo.id}'
                span.todo-remove onclick="window.handleRemoveTodoClick(this)" data-id=todo.id
                span.title - ${todo.title}
